// @flow

// $FlowIssue
const { expect } = require('chai')

const describe = global.describe
const it = global.it

describe('Gateway tests', function() {
  describe('HTTP Tunnel handling', function() {
    it('does nothing yet', async function() {
      expect(1).to.equal(1)
    })
  })
})
